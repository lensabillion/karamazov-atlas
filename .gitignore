node_modules
.next
.env.local
.env.example
*.tsbuildinfo
next-env.d.ts
.DS_Store
data/atlas.db
api/.venv/
api/**/__pycache__/

# uv's cache, for when it is pointed inside the checkout (UV_CACHE_DIR).
# api/uv.lock is deliberately NOT ignored: it is the resolution CI and the
# image replay, and an ignored lockfile is the same as no lockfile.
.uv-cache/
