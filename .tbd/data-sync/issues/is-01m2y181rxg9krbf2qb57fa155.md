---
type: is
id: is-01m2y181rxg9krbf2qb57fa155
title: "Cleanup: make the docs match the repo after the cleanup"
kind: task
status: closed
priority: 3
version: 3
labels:
  - cleanup
  - docs
dependencies: []
parent_id: is-01m2y13c1jg04xhgaxy7fmxn1m
created_at: 2026-09-19T23:50:24.285Z
updated_at: 2026-09-21T22:08:14.937Z
closed_at: 2026-09-21T22:08:14.936Z
close_reason: null
resolution: null
duplicate_of: null
---
Runs LAST, after the other five beads land. Every one of them changes a fact
that some document states.

Check and correct, against the tree rather than from memory:
- README.md — any setup or install instruction that still says pip, and the
  stated Python version.
- AGENTS.md — the tbd block is generated by `next dev`; leave it alone. Check
  anything else.
- docs/design-system.md — token names, if atlas-3w18 changed or added any. It
  is the authoritative document, so it leads and globals.css follows.
- docs/PROGRESS.md and docs/TODO.md — add the cleanup pass, as previous passes
  are recorded.
- docs/deployment-plan.md — the Dockerfile's install step changed.

Do not restate what the code already says. A document that repeats a build
command is a second copy to go stale; link to the real one where possible.

## Notes

Done in ee65100, checked against the tree rather than from memory.

- README: added an API quick start (uv sync, ingest, pytest, uvicorn). Each
  command was run from a deleted .venv before being written down. The README
  previously had no API instructions, and the obvious guess,
  `pip install -e ".[dev]"`, no longer works now that dev is a PEP 735 group.
  "six suites" is now seven. The CI section names the uv/ruff steps and the
  design guard.
- docs/PROGRESS.md: added §9 for this pass, in §8's shape, plus a changelog row.
- docs/design-system.md: the spacing rule went in with 6bc3a8b. The rule tokens
  went in with 91f8e23.
- docs/deployment-plan.md: no change needed. Its Dockerfile excerpt
  (`RUN python -m atlas.ingest`) is still what the image runs, through the venv
  on PATH.
- docs/TODO.md: deliberately unchanged. It is the record of the 9 Sep audit's
  own findings, and this pass is not one of them.
- AGENTS.md: untouched. Its tbd block is generated.
- models.py: one false clause from this branch corrected ("the generated
  client" — none exists). The docstring's older, identical premise is left to
  atlas-sqda, so the stacked atlas-ljh6 branch still merges cleanly (git
  merge-tree exit 0).
